export type TBallParams = {
  x: number;
  y: number;
  radius: number;
  color: string;
};

export type TMouseDirection = {
  prevX: number | null;
  prevY: number | null;
};

export type TMouseDirectionVelocity = {
  x: number;
  y: number;
};
